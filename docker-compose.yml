version: '3.9'

services:
  db:
    image: postgres:14-alpine
    restart: unless-stopped
    env_file:
      - ./.env
    volumes:
      - db_data:/var/lib/postgresql/data

  backend:
    build: ./backend
    restart: unless-stopped
    env_file:
      - ./.env
    depends_on:
      - db
    # The backend is not exposed directly, only through the frontend proxy
    expose:
      - "8000"

  ml_service:
    build: ./ML
    restart: unless-stopped
    env_file:
      - ./.env
    depends_on:
      - db # Assuming it might need the DB to be up, or services it interacts with do.
    expose:
      - "8000" # FastAPI app runs on port 8000 inside the container

  frontend:
    build:
      context: ./frontend
      args:
        - VITE_API_URL=${VITE_API_URL}
    restart: unless-stopped
    env_file:
      - ./.env
    depends_on:
      - backend
    ports:
      - "8080:80"
    # The nginx config proxies /api to backend

volumes:
  db_data:
    driver: local