# This file contains environment variables for the frontend.
# For local development, VITE_API_URL can be empty because of the proxy setup in vite.config.ts.
# For production builds, you might need to set this if your API is on a different domain.
VITE_API_URL=
